# P0 开发批次：2026-05-06

## 完成的功能模块

### D1 — 认领项目差异化展示
- `project-card.tsx`：plaza 卡片已认领项目加左侧 teal 边框、右上角绿点、"✦ 官方"徽章
- `project-detail-hero.tsx`：新增 `claimStatus` prop，已认领项目 Hero 顶部显示"✦ 官方认证主页"绿色徽章，背景加 teal 渐变色调
- `app/projects/[slug]/page.tsx`：传入 `claimStatus={data.claimStatus}`

### D2 — Pro 模块区块
- 新建 `components/project/project-pro-connect.tsx`：公开面向访客的联接入口卡片（官网CTA/邮件/分享），含目标用户 + 适用场景 pill，免责声明
- `project-official-info-editor.tsx`：新增合作需求 + 招聘需求两个 textarea 字段
- `app/api/projects/[id]/official-info/route.ts`：接收 `collaborationNeeds`/`hiringNeeds`，合并写入 `businessInfo` JSON 字段
- `app/projects/[slug]/page.tsx`：查询新增 `businessInfo: true`，officialInfo 类型加两个字段，渲染 ProjectProConnect（claimStatus=CLAIMED 且有联系方式时）

### S1 — AI 传播内容生成器
- 新建 `components/project/project-spread-content.tsx`：传播资产卡片，5 个 tab（一句话/100字/300字/800字/受众版本），复制按钮，状态badge，生成按钮
- `page.tsx` 中 isClaimedOwner 区域渲染 ProjectSpreadContent（在 ProjectAiContentDraft 之前）

### S2 — 分享卡片升级
- `project-share-card.tsx`：全新设计，顶部品牌色条（claimed→teal-blue渐变），"✦ 官方认证主页"徽章，2xl 项目名，精致 URL 区域
- `app/projects/[slug]/share/page.tsx`：米白背景 `bg-[#f8f8f6]`，品牌顶部 header，已认领项目显示 emerald 渐变 banner

### 全局视觉升级
- `components/home/hero.tsx`：新增"AI 时代的项目公众主页"pill，被看见/理解/联系三栏价值卡片，CTA 改为"浏览项目广场"+"认领你的项目"
- `app/page.tsx`：section 标题改为"最新入驻项目"，文案改为 AI 原生生态定位

## 待 P1 验收后继续的任务
- D3 项目广场优化（标签筛选、排序）
- D4 SEO 优化
- S3 多渠道传播内容包
- S4 项目月报自动生成
- C1 结构化联系表单
- C2 项目方收件后台
- P1 首页落地页进一步优化（样板项目展示）

## 部署注意
- `prisma migrate deploy` 无需新迁移（businessInfo 已是 Json? 字段）
- 需确认 `NEXT_PUBLIC_APP_URL` 在 Vercel 配置正确（分享卡片 URL 依赖此变量）
- vercel.json 已有 muhub.cn → www.muhub.cn 跳转 + 3个 cron 任务
